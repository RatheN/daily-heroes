<h1>Welcome to Daily Heroes.</h1>

<% if !logged_in? %>
    <%= link_to "Log In", login_path, :class => "btn btn-primary" %>
    <%= link_to 'Sign Up', signup_path, :class => "btn btn-primary" %>
<% else %>
    <h3> Welcome, <%= current_user.name %>! </h3>
<% end %>