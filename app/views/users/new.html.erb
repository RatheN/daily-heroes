<h3>Sign Up</h3>

<% if @user.errors.any? %>
  <% @user.errors.full_messages.each do |msg| %>
    <p class="error"><%= msg %></p>
  <% end %>
<% end %>

<%= render 'form' %>

<h4><%= link_to("Log In", '/login') %></h4>